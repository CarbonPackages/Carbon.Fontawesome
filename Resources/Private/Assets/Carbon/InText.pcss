.fa-icon-pointer-events {
  pointer-events: auto;
}

.fa-icon-intext {
  height: 1em;
  width: 1.25em;
  display: inline-flex;
  vertical-align: middle;
  transform: translateY(-0.1em);
  align-items: center;
  justify-content: center;
}
